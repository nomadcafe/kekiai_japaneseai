# 使用するLLMプロバイダー: openai, claude, gemini, bedrock
USE_MODEL=openai

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Claude (Anthropic)
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-3-opus-20240229

# Google Gemini
GOOGLE_API_KEY=
GEMINI_MODEL=gemini-pro

# AWS Bedrock
# 以下の形式のいずれかで設定:
# 1. 統合形式 (ACCESS_KEY|SECRET_KEY)
AWS_BEDROCK_CREDENTIALS=
# 2. 個別形式
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=ap-northeast-1
BEDROCK_MODEL=anthropic.claude-3-opus-20240229-v1:0

# LLM共通設定
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=4000

# VOICEVOX設定
# Docker環境の場合: http://voicevox:50021
# ローカル環境の場合: http://localhost:50021
VOICEVOX_URL=http://voicevox:50021

# 認証設定
# アプリケーションへのアクセスパスワード（空の場合は認証なし）
LOGIN_PASSWORD=